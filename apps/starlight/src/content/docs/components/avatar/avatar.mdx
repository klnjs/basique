---
title: Avatar
description: An element for representing a user.
---

import Showcase from '@library/Showcase.astro'
import Feature from '@library/Feature.astro'
import Features from '@library/Features.astro'
import Installation from '@library/Installation.astro'
import Attributes from '@library/Attributes.astro'
import Properties from '@library/Properties.astro'
import Avatar from './avatar.tsx'
import react from './avatar.tsx?raw'
import svelte from './avatar.svelte?raw'
import styles from './avatar.module.css?raw'

## Showcase

<Showcase
	name="avatar"
	sources={[
		{ type: 'react', title: 'React', content: react, language: 'jsx' },
		{
			type: 'svelte',
			title: 'Svelte',
			content: svelte,
			language: 'svelte'
		},
		{ type: 'css', title: 'Styles', content: styles, language: 'css' }
	]}
>
	<Avatar client:load />
</Showcase>

## Features

<Features>
	<Feature>Image loading status handling.</Feature>
	<Feature>Fallback to any content imaginable.</Feature>
	<Feature>Delay fallback to avoid flickering.</Feature>
</Features>

## Installation

<Installation name="avatar" />

## API Reference

-   [Avatar](#avatar-root)
-   [AvatarImage](#avatarimage)
-   [AvatarFallback](#avatarfallback)

### Avatar (Root)

Contains all the parts of an avatar.

<Properties
	props={[
		{ name: 'asChild', type: 'boolean', defaultValue: 'false' },
		{
			name: 'onStatusChange',
			type: 'function',
			typeAdvanced:
				"(status: 'idle' | 'loading' | 'loaded' | 'error') => void"
		}
	]}
/>

### AvatarImage

Used to render the image. The image is hidden until it has loaded. Use the root property onStatusChange, to listen to the image status.

<Properties
	props={[
		{ name: 'asChild', type: 'boolean', defaultValue: 'false' },
		{ name: 'src', type: 'string' }
	]}
/>

### AvatarFallback

Used to render the fallback while the image is loading, or when an error occurs. To prevent flickering between image and fallback use the `delay` property, to delay rendering of the fallback whilst image is loading.

<Properties
	props={[
		{ name: 'asChild', type: 'boolean', defaultValue: 'false' },
		{ name: 'delay', type: 'number' }
	]}
/>
