---
title: Icon
description: An element for creating icon components.
---

import Showcase from '@library/Showcase.astro'
import Feature from '@library/Feature.astro'
import Features from '@library/Features.astro'
import Installation from '@library/Installation.astro'
import Attributes from '@library/Attributes.astro'
import Properties from '@library/Properties.astro'
import Icon from './icon.tsx'
import react from './icon.tsx?raw'
import styles from './icon.module.css?raw'

## Showcase

<Showcase
	name="icon"
	sources={[
		{ type: 'react', title: 'React', content: react, language: 'jsx' },
		{ type: 'css', title: 'Styles', content: styles, language: 'css' }
	]}
>
	<Icon client:load />
</Showcase>

## Features

<Features>
	<Feature>Quickly make an icon by providing paths.</Feature>
	<Feature>Provide accessibility if icon is interactive.</Feature>
</Features>

## Installation

<Installation name="react-icon" />

## API Reference

-   [Icon](#icon-root)
-   [IconTitle](#icontitle)
-   [IconDescription](#icondescription)
-   [IconPath](#iconpath)

### Icon (Root)

Contains all the parts of an icon.

<ReferenceProps
	props={[{ name: 'asChild', type: 'boolean', defaultValue: 'false' }]}
/>

### IconTitle

Used to optionally render a title. This functions as an accessible label for screen readers to announce when the icon is interactive. To make the icon interactive, set the `tabIndex` of the root to 0.

<ReferenceProps
	props={[{ name: 'asChild', type: 'boolean', defaultValue: 'false' }]}
/>

### IconDescription

Used to optionally render a description. Has the same properties as the title component.

<ReferenceProps
	props={[{ name: 'asChild', type: 'boolean', defaultValue: 'false' }]}
/>

### IconPath

Used to render a path. It is possible to use multiple paths to compose an icon, this is usefull for making icons with multiple colors or having different animations for each path.

<ReferenceProps
	props={[
		{ name: 'asChild', type: 'boolean', defaultValue: 'false' },
		{ name: 'd', type: 'string' }
	]}
/>
