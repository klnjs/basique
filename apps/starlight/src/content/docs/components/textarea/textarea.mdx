---
slug: components/textarea
title: Textarea
description: A field for entering multi-line plain-text content.
---

import Showcase from '@library/Showcase.astro'
import Feature from '@library/Feature.astro'
import Features from '@library/Features.astro'
import Installation from '@library/Installation.astro'
import Attributes from '@library/Attributes.astro'
import Properties from '@library/Properties.astro'
import TextareaReact from './textarea.tsx'
import react from './textarea.tsx?raw'
import styles from './textarea.module.css?raw'

## Showcase

<Showcase
	name="textarea"
	sources={[
		{ type: 'react', source: react },
		{ type: 'styles', source: styles }
	]}
>
	<TextareaReact slot="react" client:only="react" />
</Showcase>

## Features

<Features>
	<Feature>Controlled or uncontrolled.</Feature>
	<Feature>Autosizing with min and max rows.</Feature>
</Features>

## Installation

<Installation name="react-textarea" />

## Components

- [Textarea](#textarea-root)

### Textarea

Use the `autosize` prop to enable the textarea to grow and shrink to fit its content. Use the `minRows` and `maxRows` props to set the minimum and maximum number of rows the textarea can have.

<Properties
	props={[
		{ name: 'asChild', type: 'boolean', defaultValue: 'false' },
		{
			name: 'value',
			type: 'string'
		},
		{
			name: 'defaultValue',
			type: 'string'
		},
		{
			name: 'autosize',
			type: 'boolean',
			defaultValue: 'true'
		},
		{
			name: 'minRows',
			type: 'number',
			defaultValue: '2'
		},
		{
			name: 'maxRows',
			type: 'number',
			defaultValue: 'Infinity'
		},
		{
			name: 'onChange',
			type: '(value: string) => void'
		}
	]}
/>
