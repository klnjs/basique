---
import clsx from 'clsx'
import classes from './Table.module.css'

type Props = {
	class?: string
	children: any
}

const { class: classProp, children, ...otherProps } = Astro.props
---

<tr class={clsx(classes.row, classProp, 'not-content')} {...otherProps}>
	<slot />
</tr>
