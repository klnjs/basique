---
type Props = {
	class?: string
	children: any
}

const { class: classProp, children, ...otherProps } = Astro.props
---

<tr class:list={['not-content', 'table-row', classProp]} {...otherProps}>
	<slot />
</tr>
